Frontend (Flutter) scaffold

Quick start:

1. Install Flutter: https://flutter.dev/docs/get-started/install
2. From `frontend` run:

```powershell
flutter pub get
flutter run
```

Note: This is a minimal scaffold. For a full Flutter project, run `flutter create frontend` instead.
